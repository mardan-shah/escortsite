// types/navigation.ts
export interface Language {
  code: string;
  name: string;
  flag: string;
}

export interface user {
  name?: string;
}

export interface City {
  name: string;
  icon: string;
}

export interface Country {
  name: string;
  flag: string;
}